<!DOCTYPE html>
<html lang='en'>
<head>
    <meta charset='UTF-8'>
    <meta name='viewport' content='width=device-width, initial-scale=1.0'>
    <title>Document</title>
</head>
<body>
    <ul>
        <% for (const user of users) { %>
            <li><%= `id: ${user.id}, 名前: ${user.name}, メール: ${user.email}` %></li>
        <% } %>
    </ul>
</body>
</html>